<!DOCTYPE html>
<html>
  <head>
    <title>TreasureHunt</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="mx-auto prose">
    
    <div class="flex flex-col">

      <section class="pt-8">
        <% if user_signed_in? %>
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "devise-submit" %>
        <% end %>
      </section>

      <section>
        <p class="alert"><%= alert %></p>
        <p class="notice"><%= notice %></p>
      </section>

      <main class="my-8">
        <%= yield %>
      </main>
      
    </div>

  </body>
</html>
